#include <iostream>

int main(){
    std::cout << std::endl;
    std::cout << "struct and classes are different only in one aspect that in classes all variables and methods are private by default and in struct they are all public by default. That's it!" << std::endl;


    return 0;
}